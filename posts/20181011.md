---
title: 'h5开发问题集'
date: '2018-10-11 21:08:37'
---
#### 键盘遮挡input（android浏览器）
<!--more-->
```js
     /**
        *
        *@Title: inputMode
        *@Params:
        *@Description: android 键盘弹起会遮住input标签
        *@return
        */
        function inputMode(){
            var systemType = api.systemType;
            if (systemType == "android") {
                onresize = function() {
                    if(document.activeElement.tagName=="INPUT" ) {
                        window.setTimeout(function() {
                            document.activeElement.scrollIntoViewIfNeeded();
                        },0);
                    }
                }
            }
    
        }
```
#### 视频播放状态栏隐藏

解决思路：播放页进入全屏状态，apicloud案例
```js
      /*全屏隐藏状态栏*/
      api.setFullScreen({
           fullScreen: true
      });
```
